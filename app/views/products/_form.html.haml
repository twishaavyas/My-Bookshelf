/ =form_for(@product) do |f|
/ 	if @product.errors.any?
/ 		#error_explanation
/ 			%h2 pluralize(@product.errors.count, "error") prohibitated this product from being saved:
/ 		%ul
/   	- @product.errors.full_messages.each do |msg|
/     %li= msg

/ =form_for :product, :url => products_path do |f|
/ 	%p
/ 		=f.label :title
/ 		=f.text_field :title

/ 	%p
/ 		=f.label :description
/ 		=f.text_area :description, :rows => 6

/ 	%p
/ 		=f.label :image_url
/ 		=f.text_field :image_url

/ 	%p
/ 		=f.label :price
/ 		=f.text_field :price

/ 	%p
/ 		=f.submit 
= form_for(@product) do |f|
  - if @product.errors.any?
    #error_explanation
      %h2
        = pluralize(@product.errors.count, "error")
        prohibited this product being saved:
      /#summarizes the error messages in a list
      %ul
        - @product.errors.full_messages.each do |msg|
          %li= msg
  .field
    = f.label :title
    %br/
    = f.text_field :title
  .field
    = f.label :description
    %br/
    = f.text_area :description, rows: 6
  .field
    = f.label :image_url
    %br/
    = f.text_field :image_url
  .field
    = f.label :price
    %br/
    = f.text_field :price
  .actions
    = f.submit